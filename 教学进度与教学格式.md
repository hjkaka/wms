# 教学进度与教学格式规范

> **文档用途**：记录本项目（WMS 仓储管理系统）的教学进度，并统一每一次教学输出的固定格式，保证"从零学"的连贯性与可复习性。
>
> **适用范围**：本项目后端+前端开发教学全程。
>
> *最后更新：2026-08-21*

---

## 一、统一教学输出格式（模板）

每个模块的教学输出**必须**按以下六段式结构，逐步讲解并落库/测试，不跳步。

```
## 「模块名」
### 一、需求与建立方式
   - 一句话说明这个模块做什么
   - 按顺序列出要新建/修改的类清单（Entity → DTO/VO → Mapper → Service → Controller）
### 二、逐层代码 + 逐行注释
   - 告知这个模块要在什么文件位置创建，怎么创建
   - 每层给出完整代码，每一行代码都用行内注释解释"为什么这么写"，链式引用也要详细说明怎么引用的，用的方法有什么用，传的是什么，传给谁等详细说明。
   - 坚持"先加依赖方法再使用，缺方法先补再建类"的构建顺序
### 三、核心知识点讲解
   - 用生活化比喻讲清概念（事务、索引、JWT 等）
### 四、面试常考问题 + 答案
   - Q/A 形式，覆盖原理、对比、优化、踩坑
### 五、测试验证
   - 启动服务 + 接口测试 + 查库核对
### 六、进度记录
   - 标记本模块完成，更新下方进度清单
```

### 固定开发顺序（分层架构）

任何新功能都按此顺序，与 AGENTS.md「3.4 新增功能流程」一致：

```
1. Entity（对应表）      → 2. DTO/VO（请求/响应）  → 3. Mapper 接口 + XML
4. Service 接口           → 5. ServiceImpl 实现      → 6. Controller
→ 7. SecurityConfig 放行所需路径 → 8. 启动测试
```

### 贯穿的讲解原则

1. **先方法后使用**：若新类依赖别的类方法，先建好依赖方法（或留占位注释），再建使用它的类。
2. **逐行注释**：非自明的代码必须有注释，解释"为什么"而非"是什么"。
3. **每次结束给出进度小结**：已完成 ✅ / 进行中 🔄 / 未开始 ⏸。
4. **概念教学生活化**：如事务=要么全成功要么全失败、索引=书的目录。
5. **顺带讲面试题**：每模块附 Q/A，覆盖原理、对比、优化点、常见坑。

---

## 二、当前教学进度（截至 2026-08-21）

> 后端全部完成；前端未开始。当前正卡在「第11课库存查询」——业务/控制层未建全。

| 阶段 | 模块 | 状态 | 说明 |
|---|---|---|---|
| 第1课 | 明确项目范围与功能清单 | ✅ | 10 张表、三种角色、四大核心功能已定 |
| 第2课 | 开发环境检查 | ✅ | JDK17 / Maven / MySQL / Redis 就绪 |
| 第3课 | 数据库表设计（10张表） | ✅ | `sys_user warehouse category product stock stock_in_order stock_in_item stock_out_order stock_out_item stock_log` |
| 第4课 | 后端项目结构 | ✅ | 分层架构已建 |
| 第5课 | Hello World | ✅ | `/api/hello` 运行成功 |
| 第6课 | 用户登录 + JWT 认证 | ✅ | 登录接口 + JWT 过滤器 |
| 第7课 | 仓库管理 CRUD | ✅ | 增删改查 + 分页 |
| 第8课 | 商品管理 CRUD | ✅ | 价格 BigDecimal、索引优化 |
| 第9课 | 入库管理 + 库存变动 | ✅ | 事务、单号生成、库存新增/累加，已测试 |
| 第10课 | 出库管理 + 库存校验 | ✅ | 防超卖双保险、回滚，已测试 |
| **第11课** | **库存查询 + 统计报表** | 🔄 | **进行中，见下方「待完成项」** |
| 第12-16课 | 前端搭建 + 联调 | ⏸ | 未开始 |

### 第11课「库存查询」待完成项

已建部分：`vo/StockSummaryVO.java`、`StockMapper.java`（已加两方法）、`StockMapper.xml`（已写 `summaryByProduct`）。

| 待办 | 现状 |
|---|---|
| `StockMapper.xml` 补 `summaryByWarehouse` | ⚠️ 接口声明了但缺 SQL，调用会抛 `BindingException` |
| `StockService.java` | ⚠️ 空接口 |
| `StockServiceImpl.java` | ❌ 不存在 |
| `StockController.java` | ❌ 不存在 |
| 分页查询 `searchPage/countSearch`（含 DTO、联表、CASE 预警） | ❌ 未建 |
| 库存预警 / 出入库统计报表 | ❌ 未建 |
| 测试验证 | ❌ 未做 |

### 进度同步说明

- 该进度清单是**权威进度**，与 AGENTS.md 的「后续开发计划」核对存在不一致（AGENTS.md 只勾选了登录，实际仓库/商品/出入库已全部完成）。后续以本文件为准，并同步修订 AGENTS.md。
- 每完成一个模块，回到本文件勾选 ✅ 并更新「最后更新」时间。

---

## 三、已沉淀的经验与踩坑记录（复习用）

> 这些是项目真实踩过的坑，备考/面试都可引用。

1. Lombok 必须 ≥ 1.18.30 才兼容 JDK 17，否则报 `TypeTag :: UNKNOWN`。
2. JDBC URL 用 `characterEncoding=UTF-8`，不能用 `utf8mb4`（JDBC 不识别此字符集名）。
3. MyBatis 增删改方法返回 `int`/`void`，不能返回实体对象。
4. 入库/出库必须用 `@Transactional`：任意一步失败整体回滚，避免"单子登记了但库存没变"的脏数据。
5. 出库防超卖双保险：Service 先查库存 + 扣减 SQL 带 `quantity >= #{quantity}` 条件（影响行数=0 即不足）。
6. `LIKE '%xxx%'` 前置模糊不走普通 B 树索引，需 FULLTEXT 全文索引。
7. XML 中 `<=` 须转义为 `&lt;=`，或反着写成 `>=` 更省事。
8. `map-underscore-to-camel-case=true` 自动把下划线列名映射成驼峰属性。

---

*本文件与 AGENTS.md 配套使用：AGENTS.md 管项目规范，本文件管教学进度与教学输出格式。*
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wms_backend.mapper.StockTakeItemMapper">

    <resultMap id="StockTakeItemMap" type="com.example.wms_backend.entity.StockTakeItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="systemQuantity" column="system_quantity"/>
        <result property="countedQuantity" column="counted_quantity"/>
        <result property="varianceQuantity" column="variance_quantity"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 所选仓库库存快照：只取 quantity>0 的商品作为盘点明细的账面数 -->
    <!-- 用 resultType 自动映射（product_id->productId, systemQuantity->systemQuantity） -->
    <select id="getStockSnapshot" resultType="com.example.wms_backend.entity.StockTakeItem">
        SELECT product_id AS productId, quantity AS systemQuantity
        FROM stock
        WHERE warehouse_id = #{warehouseId} AND quantity > 0
    </select>

    <insert id="batchInsert">
        INSERT INTO stock_take_item (order_id, product_id, system_quantity)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.systemQuantity})
        </foreach>
    </insert>

    <!-- 明细列表（联表商品名/编码） -->
    <select id="findByOrderId" resultType="com.example.wms_backend.vo.StockTakeItemVO">
        SELECT i.id, i.product_id AS productId, p.name AS productName, p.code AS productCode,
               i.system_quantity AS systemQuantity, i.counted_quantity AS countedQuantity,
               i.variance_quantity AS varianceQuantity, i.remark
        FROM stock_take_item i
        LEFT JOIN product p ON i.product_id = p.id
        WHERE i.order_id = #{orderId}
        ORDER BY i.id
    </select>

    <!-- 明细实体（过账时按 counted/sytem 计算盈亏） -->
    <select id="findEntitiesByOrderId" resultMap="StockTakeItemMap">
        SELECT id, order_id, product_id, system_quantity, counted_quantity, variance_quantity, remark
        FROM stock_take_item
        WHERE order_id = #{orderId}
        ORDER BY id
    </select>

    <!-- 录入实盘：counted 传 null 表示未盘点（variance 也为 null） -->
    <update id="updateCounted">
        UPDATE stock_take_item
        SET counted_quantity = #{countedQuantity},
            variance_quantity = #{countedQuantity} - system_quantity,
            remark = #{remark}
        WHERE id = #{id}
    </update>

</mapper>